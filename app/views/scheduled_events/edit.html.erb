<% content_for :title, "Editing scheduled event" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing scheduled event</h1>

  <%= render "form", scheduled_event: @scheduled_event %>

  <%= link_to "Show this scheduled event", @scheduled_event, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
  <%= link_to "Back to scheduled events", scheduled_events_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
