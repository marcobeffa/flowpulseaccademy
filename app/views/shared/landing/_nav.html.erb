<nav class="bg-white border-b border-gray-200 sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <!-- Logo -->
      <div class="flex-shrink-0 flex items-center">
        <%= link_to root_path, class: "flex items-center gap-2" do %>
          <img src="/favicons/flowpulse/favicon.ico" alt="Flowpulse" class="h-8 w-8">
          <span class="font-bold text-lg text-yellow-600">Flowpulse</span>
        <% end %>
      </div>
      <!-- Links -->
      <div class="hidden md:flex space-x-6">
        <%= link_to "Home", root_path, class: "text-gray-700 hover:text-yellow-600 font-medium" %>
        <%= link_to "Chi siamo", "/about", class: "text-gray-700 hover:text-yellow-600 font-medium" %>
        <%= link_to "Domini", root_path(anchor: "domains-grid"), class: "text-gray-700 hover:text-yellow-600 font-medium" %>
        <% if Current.user %>
          <%= link_to "Dashboard", dashboard_user_path, class: "text-gray-700 hover:text-yellow-600 font-medium" %>
        <% else %>
          <%= link_to "Accedi", new_session_path, class: "text-gray-700 hover:text-yellow-600 font-medium" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
